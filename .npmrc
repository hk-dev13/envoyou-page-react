# Netlify-specific npm configuration
# Use legacy peer deps to avoid dependency conflicts
legacy-peer-deps=true

# Increase network timeout for slow connections
fetch-timeout=60000

# Enable progress for debugging
progress=false
